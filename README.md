# innoneer.TV Homepage

Static GitHub Pages rebuild of the innoneer.TV homepage.

## Deploy

Use GitHub Pages with the repository root as the publishing source. The site has no build step:

- `index.html`
- `styles.css`
- `script.js`
- `assets/img/*`

The page supports `ZH-HK`, `CN`, and `EN` language modes in the browser.
